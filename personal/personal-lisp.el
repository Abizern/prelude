;;; personal-lisp.el --- Personal Lisp Settings
;;
;;; Commentary:
;;  Personal settings for editing Common Lisp files

;;; Code:

;; Set up clisp as the default lisp instead of ccl
(setq slime-default-lisp 'clisp)


(provide 'personal-lisp)
;;; personal-lisp.el ends here
